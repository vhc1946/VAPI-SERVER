/**
 * The CollateralForm will be a class that will allow us to dynamically generate collateral objects and return them.
 * It will take as parameters a series of data objects that it will then use to generate the collateral. 
 */
export class CollateralForm{
    constructor(){
        
    }

    /**
     * Add a container to the collateral
     */
    ADDcontainer(cont) {
        
    }

    /**
     * Remove a container from the collateral
     */
    REMOVEcontainer() {

    }

    /**
     * Add a set of controls to the collateral titlebar
     */
    ADDcontrols() {

    }

    /**
     * Remove a set of controls from the collateral titlebar
     * @param {Array} controls | array of controls to remove
     */
    REMOVEcontrols(controls=this.controls) {

    }
}